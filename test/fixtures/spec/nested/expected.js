/** @type {jQuery} */var $;

/** @constructor */
function A() {}
/**
 * @param {string} s
 * @return {string}
 */
A.prototype.a = function a(s) {
    var fn;
    /** @type {lodash} */
    var _;

    fn = _.trim;

    return fn(s);
};
